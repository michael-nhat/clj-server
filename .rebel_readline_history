1671887128826:(ns pedestal-hello1.pedestal-hello1)
1671896156549::repl/help
1671903249871:(with-open [conn (nrepl/connect :port 59258)]\n     (-> (nrepl/client conn 1000)\n         (nrepl/message {:op "eval" :code "(time (reduce + (range 1e6)))"})\n         doall      ;; `message` and `client-session` all return lazy seqs\n         pprint))
1671903284568:(require '[nrepl.core :as nrepl])
1671903327322:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(+ 2 3)"})\n         nrepl/response-values))
1671903640839:(require '[nrepl.core :as nrepl])
1671903642341:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(+ 2 3)"})\n         nrepl/response-values))
1671903706730:(:require [cider.nrepl :refer [cider-nrepl-handler]]\n            [nrepl.core :as nrepl]\n            [nrepl.server :as nrepl-server]\n            [nrepl.transport :as transport]\n            [pedestal-hello1.pedestal-hello1] ;; [nrepl.transport :refer [send]]\n            [rebel-readline.main :as rebel])
1671903838252:(:require \n \n            [nrepl.core :as nrepl]\n            [nrepl.server :as nrepl-server]\n            [nrepl.transport :as transport]\n            [pedestal-hello1.pedestal-hello1] ;; [nrepl.transport :refer [send]]\n \n [rebel-readline.main :as rebel])
1671903855360:(require '[nrepl.core :as nrepl])
1671903878688:(:require '[cider.nrepl :refer [cider-nrepl-handler]]\n            '[nrepl.core :as nrepl]\n            '[nrepl.server :as nrepl-server]\n            '[nrepl.transport :as transport]\n            '[pedestal-hello1.pedestal-hello1] ;; [nrepl.transport :refer [send]]\n            '[rebel-readline.main :as rebel]))
1671903957921:(require '[nrepl.core :as nrepl])
1671903971080:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(+ 2 3)"})\n         nrepl/response-values))
1671904024074:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(ns pedestal-hello1.pedestal-hello1)"})\n         nrepl/response-values))
1671904033536:(ns pedestal-hello1.pedestal-hello1)
1671904063682:(ns user)
1671904319344:(gensym)
1671904483612:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(ns pedestal-hello1.pedestal-hello1)"})\n         nrepl/response-values))
1671904492951:(require '[nrepl.core :as nrepl])
1671904500098:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op "eval" :code "(ns pedestal-hello1.pedestal-hello1)"})\n         nrepl/response-values))
1671904958650:(def aaa 3)
1671904995146:(def aaa 5\n  )
1671905067949:conn
1671905117849:(def aaa 9\n  )
1671905484697:(require '[nrepl.core :as nrepl])
1671905507080:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op :eval\n    :code "(range 512)"\n    :nrepl.middleware.print/stream? 1\n    :nrepl.middleware.print/quota 8})\n         nrepl/response-values))
1671905527320:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op :eval\n    :code "(range 512)"\n    :nrepl.middleware.print/stream? 1\n    :nrepl.middleware.print/quota 8})\n         \n         ))
1671905557570:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op :eval\n    :code "(print 'wer)"\n    :nrepl.middleware.print/stream? 1\n    :nrepl.middleware.print/quota 8})\n         \n         ))
1671905714001:dsfsd
1671905757210:(require '[nrepl.core :as nrepl])
1671905758458:(with-open [conn (nrepl/connect :port 7885\n                                \n                                )]\n     (-> (nrepl/client conn 1000)    ; message receive timeout required\n         (nrepl/message {:op :eval\n    :code "(print 'wer)"\n    :nrepl.middleware.print/stream? 1\n    :nrepl.middleware.print/quota 8})\n         \n         ))
1671964016151::repl/help
1671964939245::repl/keybindings
1671964982231::repl/help
